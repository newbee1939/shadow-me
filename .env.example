# Create this in OpenAI Platform
# https://platform.openai.com/api-keys
OPENAI_API_KEY=<your-api-key>

# Create this in Claude Console
# https://platform.claude.com/settings/keys
ANTHROPIC_API_KEY=<your-api-key>

# Create this in Google AI Studio
# https://aistudio.google.com
GOOGLE_GENERATIVE_AI_API_KEY=<your-api-key>

# Create this in GitHub
# https://github.com/settings/personal-access-tokens
GITHUB_PERSONAL_ACCESS_TOKEN=<your-github-pat>

# Create a Slack app and install it to your workspace
# https://api.slack.com/apps
# Required OAuth Scopes: app_mentions:read, channels:history, chat:write, im:history
SLACK_BOT_TOKEN=<xoxb-your-bot-token>
SLACK_SIGNING_SECRET=<your-signing-secret>

# Workflow Scheduler Configuration
# Set to "true" to enable periodic workflow execution
ENABLE_NEWSLETTER_CRON=false
# Cron expression for newsletter workflow (default: every Monday at 9:00 AM)
# Format: minute hour day month weekday
NEWSLETTER_CRON=0 9 * * 1
